#!/bin/sh

echo "Generating device UID ..."
UID=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 256)
echo "Done. UID:"
echo "$UID"

if [ "$1" == "write-mmc" ]; then
	echo "Flashing UID to MMC ..."
	echo -n "$UID" | dd of=/dev/mmcblk0 bs=512 seek=1
	echo "Done."
	exit 0
fi

exit $?
